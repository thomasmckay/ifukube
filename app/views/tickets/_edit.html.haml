= content_for :title do
  = "#{@ticket.system.capitalize} #{@ticket.number}"

= content_for :remove_item do
  = link_to _("Remove Ticket"), { :controller => "tickets", :action => "destroy" }, :confirm => _('Are you sure you want to remove this ticket?'), :method => :delete, :class=>"remove_item", :remote=>true

-# TODO: navigation
-#= content_for :navigation do
-#  = render_menu(1..2, user_navigation)

= content_for :content do
  = render :partial => "edit_form", :locals=>{:tickets => tickets}

= render :file => "layouts/tupane_layout"
